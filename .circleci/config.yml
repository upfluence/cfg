version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.11
    working_directory: /go/src/github.com/upfluence/cfg
    steps:
      - checkout
      - run: go get -t ./...
      - run: go test -v ./...

workflows:
  version: 2
  test:
    jobs:
      - test
